# hola probando
head(iris,2)
str(iris)
summary(iris)
plot(iris)

library(ggplot2)
ggplot(iris, aes(x = Species, y = Sepal.Length
                fill = Species))



























#Analisis de relacion
#Identificar si las variables independientes afectan a las variables independientes.

$ y= \beta $


